{
    "builds": [{
        "src": "resort_project/wsgi.py",
        "use": "@vercel/python",
        "config": {
            "maxLambdaSize": "15mb"
        }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "resort_project/wsgi.py"
        }
    ]
}
